*This project has been created as part
of the 42 curriculum by mledda.*

DESCRIPTION:
I have to recode the printf() function.
Printf writes the C string pointed by format to the standard output (stdout). If format includes format specifiers (subsequences beginning with %), the additional arguments following format are formatted and inserted in the resulting string replacing their respective specifiers.
specifiers asked:
d or i	Signed decimal integer	
u	Unsigned decimal integer	
o	Unsigned octal
x	Unsigned hexadecimal integer
X	Unsigned hexadecimal integer (uppercase)
p   Pointer address
%   % followed by another % character will write a single % to the stream.

On success, the total number of characters written is returned.

Instructions:
The principal function is ft_printf.c, the header is ft_printf.h and the makefile with all the files needed>

ressources:

https://www.youtube.com/results?search_query=ft_printf
https://www.youtube.com/watch?v=7Sph8JlRo0g